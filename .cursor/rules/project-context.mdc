---
description: 项目长期上下文：AI训练师 + 业余设计师个人网站
alwaysApply: true
---

# Project Context

## 项目定位

这是一个“AI 训练师 + 业余设计师”的个人网站项目。网站用途包括：

- 展示用户的 AI 作品与 AI 训练相关实践。
- 归纳用户的多维视觉表达，包括平面作品与建模作品。
- 呈现用户的经历、个人介绍与联系方式。
- 用户姓名：邹诗洋。
- 用户背景：产品设计出身，有平面设计经验，正在转型 AI 训练师，并对该方向有强烈兴趣。
- 作为具有强视觉记忆点的数字名片与个人作品档案馆。

## 已锁定设计方向

用户已在 4 个方向中选择：**电影级多维视觉宇宙**。

核心风格：

- 深蓝黑暗色背景。
- 冷雾空间与电影级滚动叙事。
- 暖橙色作为能量点、CTA 和活跃态。
- 细线网格、档案标签、AI 数据感。
- 作品星群、Bento/轮播作品归档、3D/建模舞台。
- 已进一步强化“AI 训练 / 数据流 / 反馈迭代”视觉隐喻：Hero 身份标签、训练流程轨道、样本滚动流、作品扫描标签、Evolution Log 时间线。
- 参考用户提供的 Skydda AI Sentinel 截图与链接，但只继承气质，不直接复刻。

## 参考来源

- 用户发送的 Skydda 风格截图。
- 参考 URL：`https://v0.app/templates/skydda-ai-sentinel-xsWGOZF9wTT?f=1&b=deployment`
- 本项目设计规范文件：`DESIGN.md`

## 设计规范要求

后续所有页面和组件都必须优先遵循项目根目录下的 `DESIGN.md`。

关键约束：

- 所有颜色必须通过 CSS 变量引用，禁止硬编码 hex。
- 中文页面必须使用 `Noto Sans SC` 等中文字体，正文行高不低于 1.7。
- 交互级别为 L3，但要注意性能边界。
- WebGL / Three.js 单页最多 1 处，并且不可见时必须暂停渲染。
- pin 场景单页最多 2 处。
- pointermove 监听必须使用 requestAnimationFrame 节流。
- 移动端必须降级复杂动效，保证无横向溢出，触摸目标不小于 44px。
- 不要做成普通简历模板、普通三列作品集或纯黑平面网页。

## 信息架构建议

首页建议结构：

1. Hero 个人宣言：AI 训练师 + 业余设计师 + 多维视觉表达。
2. 数字身份识别模块：以 AI 识别界面、数据字段、扫描动画展示邹诗洋的姓名、产品设计背景、平面设计经验与 AI 训练师转型方向。
3. AI 训练作品 / 方法：展示模型训练、提示词实验、数据标注、反馈迭代等实践。
3. 多维视觉作品归档：平面作品、视觉实验、海报/版式/品牌等。
4. 3D / 建模作品舞台：建模作品、空间构成、材质实验。
5. 经历时间线：学习、项目、工具、角色变化。
6. 关于我：个人介绍、兴趣、方法论。
7. 联系方式：邮箱、社媒、作品平台等。

## 当前技术实现

项目当前采用轻量静态前端实现：

- `index.html`：首页结构与中文占位内容。
- `styles.css`：全站视觉系统、响应式、组件样式与 CSS 动效。
- `app.js`：滚动 reveal、指针聚光、GSAP 滚动过渡、单个 Three.js 3D 舞台。
- `README.md`：本地预览说明。

外部依赖通过 CDN 引入：

- GSAP + ScrollTrigger：用于 Hero 卡片汇聚、章节标题入场与滚动动效。
- Three.js：用于“3D / 建模作品舞台”的单个 WebGL 场景。
- Google Fonts：`Noto Sans SC`、`Inter`、`JetBrains Mono`。

本地预览需使用 HTTP 服务，例如：`python -m http.server 8000`。

## 后续维护规则

- 当项目定位、设计方向、技术栈、页面结构、作品分类或用户个人信息发生变化时，必须同步更新本文件。
- 当 `DESIGN.md` 发生关键变更时，必须同步更新本文件中的相关摘要。
- 后续写代码前，应先读取本文件和 `DESIGN.md`。
