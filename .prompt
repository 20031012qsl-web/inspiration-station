请基于当前项目继续构建一个“AI 训练师 + 业余设计师”的个人网站。不要重新发散设计方向，必须严格读取并遵循以下文件：

1. `DESIGN.md`：项目视觉设计规范。
2. `.cursor/rules/project-context.mdc`：长期项目上下文。

## 项目目标

构建一个用于展示个人身份与作品的高完成度个人网站，内容包括：

- AI 作品与 AI 训练相关实践。
- 多维视觉表达，包括平面作品与建模作品归纳。
- 个人经历时间线。
- 个人介绍与联系方式。

## 已锁定风格

设计方向为：**电影级多维视觉宇宙**。

视觉气质参考用户提供的 Skydda AI Sentinel 截图和链接，但不要复刻原站文案、信息架构或完整版式。应转译为个人作品档案馆：深蓝黑、冷雾空间、暖橙能量点、细线网格、作品星群、3D 展台、AI 数据档案感。

## 构建要求

1. 先检查当前项目技术栈；如果没有现有项目，请创建一个最小可行的前端项目。
2. 优先使用现代、轻量、易维护的方案；不要引入不必要的复杂框架。
3. 严格使用 `DESIGN.md` 中定义的 CSS 变量、字体、组件规范、动效规则和响应式策略。
4. 不要硬编码颜色 hex，所有颜色都从 CSS 变量读取。
5. 页面语言以中文为主，中文字体必须包含 `Noto Sans SC`，正文行高不低于 1.7。
6. 首页结构建议为：
   - Hero 个人宣言
   - AI 训练作品 / 方法
   - 多维视觉作品归档
   - 3D / 建模作品舞台
   - 经历时间线
   - 关于我
   - 联系方式
7. Hero 必须有强视觉记忆点：作品卡片星群或类似的电影级视觉开场。
8. 首次滑动必须有一个明显钩子：卡片汇聚、场景切换或大标题横向滚动。
9. 作品展示区不能是普通等宽网格，必须使用 Bento、film strip、TiltedCard、SpotlightCard 或 3D stage 等非平庸编排。
10. L3 动效要注意性能：WebGL / Three.js 单页最多 1 处，pin 场景最多 2 处，pointermove 必须 requestAnimationFrame 节流，移动端必须降级。
11. 每个可交互元素都要有 hover、focus、active 或 disabled 状态。
12. 必须支持 `prefers-reduced-motion` 降级。
13. 移动端不得横向溢出，触摸目标不小于 44px。
14. 不要使用 Lorem Ipsum；如个人内容缺失，使用明确可替换的中文占位内容。

## 内容占位建议

如果用户没有提供真实素材和文案，请使用可替换占位内容，但要贴合项目：

- Hero 标题：`在 AI 与视觉之间，训练可被感知的表达。`
- 副标题：`我是 AI 训练师，也是一名持续探索平面与三维视觉语言的业余设计师。这里归档我的模型实验、视觉作品、建模练习与成长路径。`
- 分类：`AI 训练实验`、`平面视觉表达`、`3D / 建模作品`、`经历与方法论`
- CTA：`查看作品档案`、`了解我的方法`

## 交付要求

完成后请进行自检：

- 是否严格遵守 `DESIGN.md`。
- 是否有 3 个首页视觉爆点。
- 是否实现响应式与 reduced-motion。
- 是否没有硬编码颜色。
- 是否没有横向溢出。
- 是否更新 `.cursor/rules/project-context.mdc` 中因构建产生的新增项目事实。
